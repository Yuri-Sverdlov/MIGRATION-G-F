# HANDOFF-1 — предложение консультанта (Opus 4.8) архитектору

> Стартовый хендофф по проекту MIGRATION-G-F. Дальше проект ведёт архитектор
> (планы + `tasks/TASK.md`), кодер выполняет. Консультанта звать на спорных местах.

## Цель проекта
Пошагово перенести весь `G:\AI\` на диск **F:** (в корень `F:\`), партиями, без потерь.

## Метод (обязателен для каждой папки)
1. **Копия** — `robocopy SRC DST /E /COPY:DAT /R:1 /W:1` (гиганты: `/MT:16`, в фоне).
2. **Проверка** — число файлов src = dst. Без совпадения перенос не засчитывать.
3. **Починка путей** — два вида ссылок на старое место:
   - *внутри проектов*: жёсткие `G:\...` в `.py/.ps1/.bat/.json/.env` и т.п.;
   - *снаружи*: настройки приложений (напр. Ollama `OLLAMA_MODELS`, LM Studio `downloadsFolder`),
     ярлыки `.lnk`, `*.code-workspace`. Git-remote НЕ трогать (это URL).
4. **Удаление оригинала** на G: — только после проверки.

## Состояние партии 1 (сделано)
- 6 папок скопированы+проверены на F: (корень): `MonteCarloOptimizationKimi`,
  `OBSIDIAN-KNOWLEDGE-BASE`, `TOMATO-2-TIMERS`, `COURSES`,
  `OLLAMA-LMSTUDIO` (129 ГБ), `OllamaModels` (59 ГБ).
- Пути на F:: `OLLAMA_MODELS` user=`F:\OLLAMA-LMSTUDIO\OllamaModels`, machine=`F:\OllamaModels`;
  LM Studio `downloadsFolder`=`F:\OLLAMA-LMSTUDIO\GGUF_Models`.
- Оригиналы на G: ещё не удалены → это `TASK-01` (см. `tasks/TASK.md`).

## Грабли, найденные при партии 1 (учесть на будущее)
- **Ложные совпадения `G:\`** при сканах: строки кода вроде `"...string:\n"` дают `g:\` (Select-String регистронезависим). Отсеивать шум.
- **`.venv` непереносимы** — внутри зашит абсолютный путь; пересоздавать, не править.
- **Пути приложений — снаружи папки** (в env/конфигах в `%USERPROFILE%`), копирование папки их не чинит.
- **Хук ложно блокирует `Format-Table`/`Format-List`** (видит «format»). Кодеру — избегать их, брать `Out-String`/ручной вывод.
- **Кириллица в именах** — работать объектами, не строкой (DEV-NOTES §5).

## Рекомендации архитектору
1. Провести `TASK-01` (проверка Ollama/LM Studio → удаление 6 оригиналов).
2. Дальше резать миграцию **партиями по 5–8 папок**; каждую — по методу выше, с проверкой.
3. Перед удалением любой партии — прогонять скан жёстких `G:\`-путей и чинить.
4. Спорные/рискованные места (нестандартные приложения, БД, сервисы с путями) — эскалировать консультанту.

## Ссылки
- Общие правила среды: `F:\DEV-NOTES.md`.
- Защита от деструктива: `DEV-NOTES.md` §15 + `G:\AI\CODING_Session-Report_Layers-2-3-Hardening.md`.
